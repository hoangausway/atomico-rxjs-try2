{
  "name": "atomico-rxjs-try2",
  "description": "State management with RxJS in Atomico - fetch url",
  "version": "1.0.0",
  "source": "index.html",
  "scripts": {
    "build": "bundle index.html dist",
    "build:web-components": "bundle src/components/*-*/*-*.js web-components --external",
    "dev": "bundle index.html dist --watch --server",
    "prepare": "npm run build"
  },
  "dependencies": {
    "atomico": "^0.21.1",
    "rxjs": "^6.5.4"
  },
  "devDependencies": {
    "bundle-cli": "^0.21.5"
  }
}
